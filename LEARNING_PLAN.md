# DevOps Learning Plan — путь к Senior (v4)

**Старт:** 25.04.2026
**Цель:** Junior DevOps offer → потом Senior
**Режим:** смешанный — стартуем стабильно, через 3-4 недели смотрим реальный темп, корректируем
**Главный принцип:** учимся **когда есть силы**, а не "по графику любой ценой"

---

## Жизненный контекст

Вахтовый график 30/30:
- 30 дней межвахты — основное окно для нового материала
- 30 дней вахты — только Anki/повторение, никакого штудирования
- Отпуски — добавляют обучаемых дней, но без обязательств

Параллельно: стройка, машина, семья, друзья. Это **правильные приоритеты**, обучение не должно их вытеснять.

---

## Velocity & корректировка темпа

Считаем по **циклу 60 дней** (вахта + межвахта), не по календарной неделе.

**Базовый темп:** 2-3 юнита за цикл (это норма при таком графике).
**Ускоренный:** 4-5 юнитов за цикл (если межвахта была ровная и силы были).
**Низкий:** 1 юнит за цикл (что-то идёт не так — обсуждаем).

### Velocity Tracker

| Цикл | Период          | Юнитов | Часов  | Комментарий                                                                                                              |
|------|-----------------|--------|--------|--------------------------------------------------------------------------------------------------------------------------|
| 0    | 25.04 → 09.05   | 1      | ~10-15 | Юнит 1 завершён (8.5/10 после работы над ошибками)                                                                       |
| 1    | 04.05 → 02.06   |        |        | Текущая межвахта                                                                                                         |
| 1    | 10.05           | 1      |30 минут| с 12:30 по 13:00 читал 5 главу с конспектированием                                                                       |
| 1    | 14.05           | 1      |85 минут| с 08:35 по 10:00 читал 5 главу с конспектированием                                                                       |
| 1    | 14.05           | 1      |10 минут| с 11:25 по 11:35 читал 5 главу с конспектированием                                                                       |
| 1    | 14.05           | 1      |10 минут| с 16:50 по 17:00 читал 5 главу с конспектированием. Дочитал до 129 стр.                                                  |
| 1    | 15.05           | 1      |20 минут| с 08:25 по 08:45 читал 5 главу с конспектированием. Дочитал до 130 стр.                                                  |
| 1    | 15.05           | 1      |156 мин | с 09:04 по 11:40 читал 6 главу с конспектированием. Дочитал до 144 стр.                                                  |
| 1    | 15.05           | 1      |95  мин | с 18:25 по 20:00 читал 6 главу с конспектированием. Дочитал до 145 стр. А также беседа с claude по непонятным местам     |
| 1    | 16.05           | 1      |167 мин | с 09:00 по 11:47 читал 6 главу с конспектированием. Дочитал до 165 стр. А также беседа с claude по непонятным местам     |
| 1    | 22.05           | 1      |30  мин | с 19:05 по 19:35 читал 6 главу. Дочитал до 169 стр.                                                                      |
| 1    | 03.06 → 19.06   |        |        | Отпуск — если есть силы                                                                                                  |
| 1    | 09.06           | 1      |180 мин | с 08:40 по 11:40 читал 6-7 главу с конспектированием. Дочитал до 182 стр. А также беседа с claude по непонятным местам   |
| 1    | 09.06           | 1      | 89 мин | с 13:40 по 15:09 читал 7 главу с конспектированием. Дочитал до 198 стр. А также беседа с claude по непонятным местам     |
| 1    | 10.06           | 1      |220 мин | с 08:20 по 12:00 Выполнение практических заданий                                                                         |
| 1    | 10.06           | 1      |18  мин |с 14:30 по 14:48 читал `man systemd.service`,`man systemd.timer` — по диагонали. Ни чего не понял, у меня плохо с анг. яз.|
| 1    | 10.06           | 1      |70  мин | с 14:50 по 16:00 ответы на тест из 5 вопросов и заполнение отчета                                                        |
| 1    | 18.06           | 1      |120 мин | с 14:30 по 16:30 Работа над ошибками                                                                                     |
| 1    | 19.06           | 2      |103 мин | с 14:30 по 16:13 Изучение документации к юниту, создание Anki, чтение про OSI и TCP/IP глава 9 дочитал до 220 стр.       |
| 1    | 20.06 → 02.07   |        |        | Вахта — Anki                                                                                                             |
| 1    | 20.06           | 2      |120 мин | с 15:00 по 17:00 чтение про OSI и TCP/IP глава 9 дочитал до 231 стр.                                                     |
| 1    | 21.06           | 2      |120 мин | с 08:20 по 10:20 чтение про OSI и TCP/IP глава 9 - 10 дочитал до 267 стр.                                                |
| 1    | 24.06           | 2      |70  мин | с 09:05 по 10:15 чтение про OSI и TCP/IP глава 10 дочитал до 273 стр.                                                    |
| 1    | 25.06           | 2      |69  мин | с 07:42 по 08:51 чтение про OSI и TCP/IP глава 10 дочитана до 279 стр.                                                   |
| 1    | 25.06           | 2      |119 мин | с 09:00 по 10:59 просмотр видео по OSI и TCP/IP  https://www.youtube.com/@AlekOS/playlists                               |
| 1    | 25.06           | 2      |30  мин | с 18:05 по 18:35 просмотр видео по DNS: типы записей  https://www.youtube.com/watch?mCWhjmnL3A                           |
| 1    | 25.06           | 2      |18  мин | с 18:40 по 18:58 просмотр конфигурации собственного проекта с DNS сервером                                               |
| 1    | 26.06           | 2      |120 мин | с 09:00 по 11:00 просмотр видео про TLS, беседа с claude по TLS                                                          |
| 1    | 27.06           | 2      |266 мин | с 07:30 по 11:56 чтение man dig, а также обучение этому чтению на английском вместе с claude                             |
| 1    | 27.06           | 2      |20  мин | с 12:00 по 12:20 создание anki-карточек по трудным английским словам   + повторение всех карточек на англ яз             |
| 1    | 27.06           | 2      |119 мин | с 15:01 по 17:00 чтение man dig, а также обучение этому чтению на английском вместе с claude                             |
| 1    | 27.06           | 2      |89  мин | с 18:26 по 19:55 чтение man dig, а также обучение этому чтению на английском вместе с claude                             |
| 1    | 27.06           | 2      |7   мин | с 07:40 по 07:47 повторение anki-карточки                                                                                |
| 1    | 28.06           | 2      |180 мин | с 07:50 по 11:50 чтение man dig, а также обучение этому чтению на английском вместе с claude                             |
| 1    | 28.06           | 2      |20  мин | с 14:10 по 14:30 man dig дочитан и переведен                                                                             |
| 1    | 28.06           | 2      |13  мин | с 14:40 по 14:53 повторение anki-карточки                                                                                |
| 1    | 29.06           | 2      |40 мин  | с 07:30 по 08:10 выполнение экспериментов                                                                                |
| 1    | 01.07           | 2      |40 мин  | с 07:30 по 08:10 выполнение экспериментов                                                                                |
| 1    | 02.07           | 2      |40 мин  | с 07:30 по 08:10 выполнение экспериментов                                                                                |
| 2    | 04.07 → 02.08   |        |        | Межвахта                                                                                                                 |
| 2    | 04.07           | 2      |165 мин | с 07:30 по 11:15 выполнение экспериментов                                                                                |
| 2    | 04.07           | 2      |120 мин | с 17:10 по 19:10 выполнение экспериментов, ответ на тест, заполнение                                                     |
| 2    | 04.07           | 2      |30  мин | с 19:20 по 19:50 проверка claude, работа над ошибками - сделал anki по пробелам в знаниях                                |
| ...  |                 |        |        |                                                                                                                          |

**Анализ раз в 60 дней (конец цикла):**
- 4-5 юнитов → шёл в ускоренном, можно держать темп
- 2-3 юнита → план в норме
- 0-1 юнит → разговор: что мешает, не пора ли пауза

### Правила, которые остаются жёсткими

Это не "темпы и сроки", а качество — и оно не обсуждается:

1. ✅ Тест юнита ≥80% перед переходом дальше
2. ✅ Артефакты в Git (без артефакта юнит не закрыт)
3. ✅ Заметки с разделом "Ключевые выводы"
4. ✅ Можешь объяснить тему 5 минут устно

Если хоть одно не выполнено — **стоп, доделываем**, даже если хочется бежать дальше.

### Правила, которые гибкие (не загоняем себя)

- Сколько часов в неделю — **по самочувствию**
- В какие дни — **когда удобно**
- Сколько глав книги — **сколько прочитал, столько прочитал**, не больше плана
- Отпуск активный или для отдыха — **по силам в момент отпуска**

---

## Стратегия "мозг свежий vs мозг устал"

Главный принцип эффективности при твоём графике.

### На свежую голову (утро межвахты, выходные, отпуск)

- Изучение **новых концепций**
- Практические эксперименты, написание кода
- Чтение книг
- Создание Anki-карточек по новому материалу

### Когда устал (вечер после стройки, конец недели межвахты)

- Просмотр своих заметок
- Лёгкие YouTube-разборы по уже знакомому
- Anki — повторение

### На вахте (после 12-часовой смены)

- **Только Anki** — 10-20 минут, когда есть окно
- Иногда — перечитывание заметок с прошлой межвахты
- **Никаких** новых тем, новых книг, нового кода
- Цель вахты — не забыть пройденное, а не узнать новое

### Anki как multiplier

Если на каждой межвахте **создаёшь** карточки по пройденному, а на вахте **повторяешь** их — на следующей межвахте не тратишь время на "вспомнить, что было". Сразу идёшь дальше. Это даёт реальный +20% к усвоению при том же количестве часов.

---

## Система тестирования

**Уровень 1 — Self-check (15 мин)**: 5 вопросов в конце юнита, отвечаешь письменно в `PROGRESS_LOG.md` без гугла, потом сверяешь с эталоном. Шкала 0/1/2 за вопрос, максимум 10.

**Уровень 2 — Месячный экзамен со мной (30-45 мин)**: 15 вопросов в чате без подсказок. Раз в цикл (60 дней), не по календарю.

**Уровень 3 — Этапный экзамен**: при закрытии Этапа 1 — практическое задание + наблюдение.

**Anti-cheating:** на тестах никакого гугла, AI, подсматривания. Это для тебя — не для меня.

**Правило:** самооценку ставь ПОСЛЕ теста, не до.

---

# ЭТАП 1. ФУНДАМЕНТ (юниты 1-12)

**Цель:** уверенно администрировать Linux, настраивать сети и веб-инфраструктуру, писать bash, владеть Git.

Юниты помечены по типу нагрузки:
- 🧠 — нужна свежая голова, делать утром или на выходных
- 📺 — можно вечером, не очень напряжно
- 🔁 — материал для повторения на вахте

---

## Юнит 1 — Linux: процессы, ФС, права ✅ ПРОЙДЕН

**Итоговый балл:** 8.5/10 (тест 6/10 → работа над ошибками довела до 8.5)

**Теория (3-4ч):**
- [x] "How Linux Works" (Brian Ward) — главы 1-4
- [x] Видео по Linux file system

**Практика (3-4ч):**
- [x] Виртуалка на VirtualBox
- [x] SSH-ключи, отключи парольный вход, поменяй порт SSH
- [x] Не-root пользователь с sudo
- [x] Используй: `ps`, `top`, `htop`, `lsof`, `kill`, `chmod`, `chown`, `find`, `du`, `df`
- [x] Права: rwx, suid, sgid, sticky bit
- [x] **Работа над ошибками:** эксперименты hard/symlink, page cache, deleted files

**Артефакт:** `notes/unit-01-linux-basics.md` — есть в Git

### Тест юнита 1 (мои ответы и работа над ошибками — сохранены)

[свернуто для краткости — детали в предыдущей версии плана и в логе]

<details><summary>Эталонные ответы</summary>

1. SUID-бит (4) + 755. Файл запускается с правами владельца, не запустившего. Пример: `passwd`.
2. Hard link = второе имя того же inode. Symlink = отдельный файл-указатель. При удалении оригинала symlink становится битым (dangling).
3. `ps aux --sort=-%mem | head` или `ps -eo pid,user,%mem,cmd --sort=-%mem | head`.
4. `df` — что говорит ядро (на уровне ФС). `du` — фактический размер файлов. Расхождение — когда удалённый файл держится открытым процессом. Поиск: `du -h --max-depth=1 / 2>/dev/null | sort -h`.
5. (1) Web-консоль провайдера; (2) `systemctl status sshd`; (3) `/etc/ssh/sshd_config`; (4) `ufw status` / `iptables -L`; (5) `journalctl -u sshd -n 50`.

</details>

---

## Юнит 2 🧠 — systemd, логи, cron 🟢 ЗЕЛЁНЫЙ СВЕТ

**Тип нагрузки:** свежая голова обязательна (новая практика, написание unit-файлов)

**Контекст по моему бэкграунду:**
- systemd: концептуально знаю, **руками не писал unit-файлы** → проходим целиком
- cron: настраивал когда-то для GLPI, забыл → беглое восстановление

**Теория (3ч):**
- [x] "How Linux Works" — главы 5-7 (boot, systemd) 📺
- [x] `man systemd.service`, `man systemd.timer` — по диагонали 📺

**Практика (4ч):** 🧠
- [x] Свой systemd unit для Python-скрипта (пишет в лог раз в N секунд)
- [x] `journalctl` с фильтрами: `--since`, `-u`, `-f`, `-p`, `-n`
- [x] Cron: ежедневный бэкап `/etc` в `/var/backups/etc-YYYYMMDD.tar.gz`
- [x] То же через **systemd timer** — сравни два подхода

**Дополнительное задание (для закрытия пробела с зависимостями):**

Цепочка из 2-3 юнитов:
- `myapp.service` — Python-скрипт пишет в `/var/log/myapp.log`. Зависит от `network-online.target`
- `myapp-rotator.timer` + `myapp-rotator.service` — раз в минуту проверяет лог-файл, и если он больше 1MB — архивирует его

После настройки:
- `systemctl status myapp.service`
- `systemd-analyze critical-chain myapp.service`
- `systemctl list-dependencies myapp.service`

**Артефакты в Git** (`artifacts/unit-02-systemd/`):
- [x] `myapp.service`
- [x] `myapp.py`
- [x] `myapp-rotator.service`
- [x] `myapp-rotator.timer`
- [x] `backup-etc.sh`
- [x] `backup-via-cron.txt`
- [x] `backup-etc.service`
- [x] `backup-etc.timer`

**Заметки в Git:** `notes/unit-02-systemd.md` (раздел "Ключевые выводы для дежурства" + эксперименты)

### Тест юнита 2

1. systemd timer vs cron — минимум 3 различия.
2. Команда: логи nginx за последний час, только error-уровень.
3. `Restart=on-failure` vs `Restart=always` — разница.
4. Как сделать, чтобы сервис стартовал после network и БД?
5. Cron job не работает. Где смотреть логи на Ubuntu?

<details><summary>Эталонные ответы</summary>

1. (а) timer пишет в journald, cron шлёт письма; (б) timer поддерживает зависимости от unit'ов; (в) `Persistent=true` запустит пропущенное после downtime; (г) точность таймера до миллисекунд.
2. `journalctl -u nginx --since "1 hour ago" -p err`. Уровни: emerg(0)…debug(7).
3. `on-failure` — рестарт только при ненулевом exit или kill. `always` — при любом завершении (включая `systemctl stop`).
4. `[Unit]` секция: `After=network-online.target postgresql.service` + `Wants=network-online.target` (или `Requires=postgresql.service`).
5. `journalctl -u cron`, `/var/log/syslog`. Типичные баги: минимальный PATH, CWD=`/`, нет переменных окружения.

</details>

### Anki-карточки для юнита 2 🔁 (создавай по ходу — будут материалом для вахты)

- "PID 1 в современном Linux" → systemd
- "Файл systemd unit для пользовательских сервисов где?" → `/etc/systemd/system/*.service`
- "После изменения unit-файла что сделать?" → `systemctl daemon-reload`
- "Различие After= vs Requires=" → After задаёт **порядок**, Requires — **зависимость существования**
- "Restart=on-failure vs always" → on-failure только при ненулевом exit; always при любом
- "Логи nginx за час с уровнем error" → `journalctl -u nginx --since "1 hour ago" -p err`
- "Watch логов в реальном времени" → `journalctl -u <unit> -f`
- "Type=oneshot" → запустился, отработал, завершился
- "Type=notify" → сервис сам сообщает systemd когда готов через sd_notify

---

## Юнит 3 📺/🧠 — Сети: TCP/IP, DNS

**Тип нагрузки:** смешанный
- 📺 DNS-теорию знаю — пробегаю вечером
- 🧠 Инструменты диагностики (tcpdump, dig +trace) — на свежую голову
- 🔁 Английский словарь — для Anki, повторение в любое время

**Контекст:**
- DNS-записи (A, AAAA, CNAME, MX, NS, PTR, SRV, SOA, TXT) — знаю хорошо
- Поднимал DNS slave → DNS-теория в значительной части повторение
- Инструменты диагностики могут быть слабее → основной фокус сюда

### Теория (3ч) 📺

- [x] OSI и TCP/IP — освежить. Особое внимание: что такое L2/L3/L4/L7, где живут IP, TCP, HTTP
- [x] DNS: типы записей, рекурсивный резолвинг (если уверен — пробежать)
- [x] **Новое для тебя:** что такое TLS на уровне handshake (концептуально, без криптографии)

**Рекомендую:**
- [x] Видео "TCP/IP and Subnet Masking" — любой канал, 20 минут
- [X] Документация на dig: `man dig` — попытайся читать на английском (см. словарь ниже)

---

### Эксперимент 1 🧠 — DNS-резолвинг изнутри (`dig +trace`)

**Что делаем:** проследить весь путь резолвинга от корневых серверов до твоего домена.

**Команды:**
```bash
# Чистый трейс с самого корня
dig +trace eakramar.ru

# Сравни с обычным запросом
dig eakramar.ru

# Спроси конкретный авторитетный сервер напрямую (имя возьми из вывода trace)
dig @ns1.cloudflare.com eakramar.ru

# Посмотри что закэшировал твой локальный резолвер
dig eakramar.ru
# Время ответа во второй раз будет короче — почему?
```

**Что записать в `notes/unit-03-network-dns.md`:**
- Цепочка: root → TLD `.ru` → authoritative servers Cloudflare → твой ответ
- Сколько шагов? Сколько серверов?
- Что значит поле `Query time:` в выводе?
- Почему первый запрос медленный, второй мгновенный?

**Вопросы для самопроверки:**
- Если корневые сервера упадут — продолжит работать твой сайт? (Подсказка: подумай про TTL и кэш)
- Что произойдёт если в твоём NS-record указать несуществующий сервер? Сколько ждёт резолвер?

---

### Эксперимент 2 📺 — Все типы DNS-записей на твоём домене

**Что делаем:** запросить разные типы записей и понять что они отдают.

**Команды:**
```bash
dig eakramar.ru A          # IPv4
dig eakramar.ru AAAA       # IPv6 (может быть пусто — норма для большинства)
dig eakramar.ru MX         # mail servers
dig eakramar.ru NS         # name servers (авторитетные)
dig eakramar.ru TXT        # текстовые записи (SPF, DKIM, верификации)
dig eakramar.ru SOA        # Start of Authority — техническая запись зоны
dig eakramar.ru CAA        # Certificate Authority Authorization
dig eakramar.ru ANY        # все записи (может быть отфильтровано провайдером)

# Reverse lookup (PTR)
dig -x 1.1.1.1
dig -x 8.8.8.8
```

**Что записать:**
- Какие записи есть, какие пусты
- Особое внимание на SOA — там минимум 7 полей (serial, refresh, retry, expire, minimum, mname, rname). Что означает каждое?
- TTL для каждой записи — почему у разных записей разный TTL?

**Вопросы:**
- Зачем нужна CAA-запись? (Подсказка: связано с Let's Encrypt, юнит 4)
- Что произойдёт если уменьшить TTL до 60 секунд перед миграцией IP?

---

### Эксперимент 3 🧠 — tcpdump: захват HTTP и DNS трафика

**Что делаем:** увидеть **что реально летает по сети**, а не только что показывает браузер.

**Подготовка:**
```bash
sudo apt install tcpdump  # если ещё нет
```

**Сценарий А — DNS запросы:**
```bash
# Терминал 1 — захват DNS трафика (порт 53)
sudo tcpdump -i any -n port 53 -A

# Терминал 2 — генерируем DNS запрос
dig eakramar.ru
nslookup google.com
```

**Сценарий Б — HTTP трафик:**
```bash
# Терминал 1 — захват HTTP (порт 80)
sudo tcpdump -i any -n port 80 -A -s 0

# Терминал 2 — простой HTTP запрос
curl http://neverssl.com
# (neverssl.com специально не редиректит на HTTPS, удобно для эксперимента)
```

**Сценарий В — HTTPS (увидишь handshake, но не контент):**
```bash
# Терминал 1
sudo tcpdump -i any -n port 443 -A -s 0 | head -100

# Терминал 2
curl https://google.com
```

**Что записать:**
- В DNS-трафике видишь имена доменов в открытом виде? (да, DNS без DoH/DoT — plaintext)
- В HTTP видишь содержимое страницы? (да)
- В HTTPS видишь что-то осмысленное? (нет, после handshake — шифр)
- Что такое SYN, SYN-ACK, ACK в выводе tcpdump? (это TCP 3-way handshake)

**Полезные флаги tcpdump:**
- `-i any` — слушать все интерфейсы
- `-n` — не резолвить имена (быстрее, чище вывод)
- `-A` — печатать payload в ASCII
- `-s 0` — захватывать пакет целиком (без обрезки)
- `-c 10` — захватить только 10 пакетов и выйти
- `-w file.pcap` — сохранить в файл (потом можно открыть в Wireshark)

**Вопросы:**
- Сколько пакетов летит в `dig eakramar.ru`? (обычно 2: запрос + ответ)
- Что такое UDP в DNS-выводе? Почему DNS на UDP, а не TCP?
- Когда DNS использует TCP вместо UDP? (Подсказка: ответ >512 байт, или AXFR — зонный трансфер)

---

### Эксперимент 4 📺 — traceroute vs mtr: путь пакета

**Что делаем:** увидеть путь сетевого пакета через интернет.

**Команды:**
```bash
# Установка mtr если нет
sudo apt install mtr-tiny

# Классический traceroute
traceroute google.com
traceroute -I google.com  # ICMP (как ping) вместо UDP

# mtr — то же, но в реальном времени с потерями пакетов
mtr google.com           # интерактивный режим, выйти q
mtr -r -c 10 google.com  # один отчёт за 10 циклов

# Полезно для диагностики — проверь несколько целей
mtr -r -c 5 1.1.1.1      # Cloudflare DNS
mtr -r -c 5 8.8.8.8      # Google DNS
mtr -r -c 5 eakramar.ru  # твой домен через CF
```

**Что записать:**
- Сколько хопов до Google? До твоего домена?
- На каких хопах есть потери пакетов (loss%)?
- Где находится узкое место по latency?
- Чем mtr принципиально лучше traceroute для диагностики?

**Вопросы:**
- Почему некоторые хопы показывают `*` (звёздочки)? (Подсказка: ICMP filtering)
- Если на 3-м хопе loss 20%, но на 10-м хопе loss 0% — где реально проблема? (Подсказка: некоторые роутеры просто не отвечают на ICMP, но трафик через них идёт)

---

### Эксперимент 5 📺 — `ss`: сокеты и соединения

**Что делаем:** изучить рабочий инструмент для диагностики "кто что слушает, кто куда подключён".

**Команды:**
```bash
# Все TCP соединения с PID процессов, без DNS-резолвинга
ss -tnp

# Listening сокеты (порты, которые слушают сервисы)
ss -tlnp

# То же для UDP
ss -ulnp

# Все listening, и tcp и udp
ss -tulnp

# Соединения в состоянии ESTABLISHED
ss -tn state established

# Сколько соединений к конкретному порту
ss -tn '( dport = :443 or sport = :443 )' | wc -l

# Сравни со старым netstat (если установлен)
netstat -tlnp 2>/dev/null
```

**Что записать:**
- Какие порты слушает твоя система?
- Что такое состояние `LISTEN`, `ESTABLISHED`, `TIME-WAIT`?
- Какой процесс слушает порт 22?
- Что показывает `Local Address` и `Peer Address`?

**Вопросы:**
- Что значит `0.0.0.0:22` vs `127.0.0.1:22` в Local Address? (важный для безопасности момент)
- Если видишь много `TIME-WAIT` — это плохо? (Подсказка: обычно нормально, это TCP в порядке завершает соединение)

---

### Артефакты для Git

```
notes/unit-03-network-dns.md   # все эксперименты с командами, выводом и анализом
```

Структура заметок:
```markdown
## Ключевые выводы для дежурства
- (10-15 самых важных команд и фактов)

## Эксперимент 1: DNS-резолвинг через dig +trace
[команды, вывод, "что я понял"]

## Эксперимент 2: Все типы DNS-записей
...
```

### Тест юнита 3

1. 10 шагов, что происходит между вводом `https://google.com` и получением ответа.
2. CNAME vs A. Когда CNAME использовать **нельзя**?
3. Команда: какие name-серверы у `example.com`?
4. Домен резолвится по dig, но в браузере — нет. 4 причины.
5. Команда: все TCP-соединения с PID, без DNS-резолвинга.

<details><summary>Эталонные ответы</summary>

1. (1) DNS-кэш браузера; (2) stub resolver ОС; (3) рекурсивный DNS; (4) корневые → TLD → authoritative; (5) A/AAAA получены; (6) TCP handshake (SYN/SYN-ACK/ACK); (7) TLS handshake с SNI; (8) проверка сертификата; (9) HTTP GET; (10) ответ; (11) запросы к ресурсам; (12) рендер.
2. A = домен → IP. CNAME = домен → другой домен. **Нельзя на apex** (корне) — RFC запрещает CNAME с другими записями (SOA, NS обязательны на apex). Решения: ALIAS/ANAME у провайдера или CNAME flattening (как делает Cloudflare).
3. `dig NS example.com +short` или `dig +trace example.com`.
4. (а) DNS-кэш у провайдера/ОС/браузера ещё держит старое; (б) другой DNS-resolver у пользователя; (в) фаервол блокирует 80/443 для этого IP; (г) DNSSEC проблемы; (д) GeoDNS отдаёт другой IP в разных регионах; (е) HTTPS-проблема (сертификат, SNI); (ж) Cloudflare proxied — проксирует, но IP пользователя в чёрном списке CF.
5. `ss -tnp` (`-t` TCP, `-n` без резолвинга, `-p` процессы).

</details>

---

### 📖 Мини-словарь технического английского для юнита 3

**Принцип использования:**
1. Не заучивай списком. Каждое слово — отдельная Anki-карточка
2. **Front:** английское слово/фраза в контексте (как в man-странице)
3. **Back:** перевод + краткое объяснение по-русски
4. Делай 5-7 карточек в день, не больше
5. **Главное:** когда читаешь `man dig`, `man ss`, `man tcpdump` — узнавай эти слова. Слово, увиденное в реальном тексте, запоминается в 10 раз лучше списка

#### 🌐 Сеть — базовые термины

| Английский            | Русский                                      | Где встречается              |
|-----------------------|----------------------------------------------|------------------------------|
| **packet**            | пакет (единица данных в сети)                | везде в сетях                |
| **payload**           | полезная нагрузка пакета (без заголовков)    | tcpdump, документация        |
| **header**            | заголовок пакета                             | HTTP, TCP, IP                |
| **hop**               | переход (один промежуточный роутер)          | traceroute, mtr              |
| **route**             | маршрут                                      | `ip route`, routing tables   |
| **gateway**           | шлюз (роутер по умолчанию)                   | сетевая настройка            |
| **interface**         | сетевой интерфейс (eth0, wlan0)              | `ip a`, `ifconfig`           |
| **socket**            | сокет (комбинация IP+порт)                   | `ss`, программирование       |
| **port**              | порт (числовой идентификатор сервиса)        | 80, 443, 22                  |
| **bind**              | привязать сервис к порту/адресу              | "service binds to port 80"   |
| **listen**            | слушать (ждать соединений)                   | `ss -l`, listening sockets   |
| **connection**        | соединение                                   | TCP connection               |
| **session**           | сессия (более высокоуровневое соединение)    | SSH session                  |
| **handshake**         | рукопожатие (установление соединения)        | TCP handshake, TLS handshake |
| **negotiation**       | согласование (параметров соединения)         | TLS negotiation              |
| **timeout**           | тайм-аут (превышение времени ожидания)       | везде                        |
| **retry**             | повтор попытки                               | DNS, TCP retransmission      |
| **acknowledge / ACK** | подтвердить получение                        | TCP ACK                      |
| **bandwidth**         | пропускная способность канала                | network monitoring           |
| **latency**           | задержка                                     | ping, mtr                    |
| **throughput**        | реальная скорость передачи                   | benchmarking                 |
| **loss**              | потери (пакетов)                             | mtr loss%                    |

#### 📡 DNS — специфичные термины

| Английский             | Русский                                               | Где                         |
|:-----------------------|:------------------------------------------------------|:----------------------------|
| **resolve**            | разрешить (имя в IP)                                  | DNS resolution              |
| **resolver**           | резолвер (программа, которая делает резолвинг)        | /etc/resolv.conf            |
| **lookup**             | поиск (запрос к DNS)                                  | `nslookup`, "DNS lookup"    |
| **forward lookup**     | прямой поиск (имя → IP)                               | A, AAAA records             |
| **reverse lookup**     | обратный поиск (IP → имя)                             | PTR record, `dig -x`        |
| **authoritative**      | авторитетный (сервер, владеющий зоной)                | dig output                  |
| **recursive**          | рекурсивный (резолвер, обходящий цепочку за тебя)     | 8.8.8.8, 1.1.1.1            |
| **delegation**         | делегирование (передача зоны другому серверу)         | NS records                  |
| **zone**               | зона (часть DNS, управляемая одним сервером)          | DNS zone                    |
| **glue record**        | связующая запись (IP для NS, чтобы избежать рекурсии) | при делегации               |
| **TTL (Time To Live)** | время жизни записи в кэше                             | каждая DNS-запись           |
| **propagation**        | распространение (обновлений DNS по миру)              | "DNS propagation takes 24h" |


#### 🔧 Команды — слова из вывода и man-страниц

| Английский   | Русский                           | Контекст         |
|:-------------|:----------------------------------|:-----------------|
| **flag**     | флаг (опция командной строки)     | `-l`, `-v`       |
| **option**   | опция                             | то же что флаг   |
| **argument** | аргумент команды                  | `cp source dest` |
| **verbose**  | подробный вывод                   | `-v` flag        |
| **quiet**    | тихий режим (без лишнего вывода)  | `-q` flag        |
| **silent**   | полностью без вывода              | `--silent`       |
| **output**   | вывод                             | stdout           |
| **input**    | ввод                              | stdin            |
| **stream**   | поток (данных)                    | streams в Unix   |
| **redirect** | перенаправить (поток)             | `>`, `<`, `|`    |
| **pipe**     | канал между командами             | `|`              |
| **filter**   | фильтр                            | `grep`, `awk`    |
| **match**    | соответствовать (паттерну)        | regex match      |
| **pattern**  | шаблон, паттерн                   | regex pattern    |
| **wildcard** | подстановочный символ             | `*`, `?`         |
| **escape**   | экранировать (специальный символ) | `\n`, `\"`       |


#### 🐧 Linux/sysadmin — частые слова

| Английский    | Русский                       | Где                           |
|:--------------|:------------------------------|:------------------------------|
| **wedge**     | "застрять" (про процесс)      | "process wedged"              |
| **stuck**     | застрял                       | "stuck in D state"            |
| **hang**      | зависнуть                     | "command hangs"               |
| **crash**     | падение, креш                 | crash logs                    |
| **fail**      | провалиться, упасть           | exit codes                    |
| **succeed**   | выполниться успешно           | "command succeeded"           |
| **abort**     | прервать                      | abort signal                  |
| **terminate** | завершить (процесс)           | SIGTERM                       |
| **spawn**     | породить (процесс-потомок)    | "spawn child process"         |
| **fork**      | разветвление процесса         | fork() syscall                |
| **exec**      | запустить (заменить процесс)  | exec() syscall                |
| **kill**      | убить (процесс)               | `kill -9`                     |
| **signal**    | сигнал (процессу)             | SIGKILL, SIGTERM              |
| **graceful**  | штатный (shutdown)            | graceful shutdown             |
| **idle**      | простаивающий                 | idle process                  |
| **busy**      | загруженный, занятый          | "device is busy"              |
| **stale**     | устаревший                    | stale NFS handle, stale cache |
| **dangling**  | повисший, болтающийся         | dangling symlink              |
| **orphan**    | сирота (процесс без родителя) | orphaned process              |
| **zombie**    | зомби (процесс)               | zombie process                |


#### 📜 Сертификаты и безопасность (пригодится для юнита 4)

| Английский       | Русский                |                           |
|:-----------------|:-----------------------|:--------------------------|
| **issue**        | выдать (сертификат)    | "issued by Let's Encrypt" |
| **revoke**       | отозвать               | revoked certificate       |
| **expire**       | истечь                 | certificate expired       |
| **renew**        | продлить               | certbot renew             |
| **trust**        | доверять               | trusted CA                |
| **chain**        | цепочка (сертификатов) | certificate chain         |
| **root**         | корневой               | root certificate          |
| **intermediate** | промежуточный          | intermediate CA           |
| **encrypt**      | шифровать              |                           |
| **decrypt**      | расшифровать           |                           |
| **cipher**       | шифр                   | TLS cipher suite          |
| **key**          | ключ                   | private/public key        |
| **passphrase**   | парольная фраза        | SSH key passphrase        |


#### Готовые 10 Anki-карточек на старт

1. `dangling symlink` → "висящая" символьная ссылка, указывающая на несуществующий файл
2. `to bind to port 80` → привязать сервис к 80-му порту
3. `verbose output` → подробный вывод (часто `-v` флаг)
4. `process hangs` → процесс зависает (не отвечает, но не упал)
5. `graceful shutdown` → корректное завершение (с сохранением состояния)
6. `to resolve a hostname` → разрешить имя хоста (получить IP по имени)
7. `authoritative DNS server` → авторитетный DNS-сервер (отвечает за свою зону)
8. `recursive resolver` → рекурсивный резолвер (обходит цепочку DNS за тебя)
9. `TCP handshake` → рукопожатие TCP (SYN → SYN-ACK → ACK)
10. `packet loss` → потери пакетов в сети

#### Что делать с английским в юните 3

**Минимум:** когда видишь незнакомое слово в `man dig`, `man ss`, `man tcpdump` — выпиши, переведи, сделай Anki-карточку. Не запоминай списком — запоминай в контексте.

**Норма:** 5-7 новых слов в день из man-страниц этого юнита. Перечитывай Anki по вечерам или на вахте.

**Чего не делать:**
- Не учи весь словарь сразу — забудешь
- Не пытайся переводить каждое слово при чтении — учись понимать **контекст**
- Не пугайся длинных предложений в man — большинство технических текстов написаны по шаблону "глагол + объект + параметры"


---

## Юнит 4 🧠 — Nginx, HTTPS, reverse proxy

**Тип нагрузки:** свежая голова обязательна — всё новое: конфиги nginx, certbot, TLS

**Контекст:**
- nginx: знаю что веб-сервер, **руками конфиги не писал** → проходим целиком
- certbot: вообще не знаю → с нуля
- Домен eakramar.ru на Cloudflare → нужно правильно переключить режим

### Особенность для меня: домен `.ru` + Cloudflare + Let's Encrypt

**Проблема:** Let's Encrypt (LE) валидирует владение доменом двумя способами:
- **HTTP-01 challenge:** LE делает HTTP-запрос к `http://твой-домен/.well-known/acme-challenge/TOKEN`. Твой сервер должен ответить правильным контентом. Требует чтобы LE-валидатор (сидит в США/ЕС) мог достучаться до твоего сервера на 80 порту.
- **DNS-01 challenge:** ты создаёшь TXT-запись `_acme-challenge.твой-домен` с токеном. LE проверяет DNS. Не требует чтобы сервер был доступен извне.

**Для `.ru` VPS у российских провайдеров** HTTP-01 иногда работает, иногда нет — зависит от маршрутизации. Рабочее решение: **DNS-01 через Cloudflare API**. Плюс — не открываешь 80 порт, минус — надо создать API-токен CF.

**Что делать по умолчанию:**
1. Сначала попробуй HTTP-01 (проще). Если сработает — не усложняй.
2. Если не сработает или хочешь надёжности — переходи на DNS-01 через Cloudflare API.

### Что важно про Cloudflare-режимы для этого юнита

**Cloudflare имеет два режима для A/AAAA записей:**
- 🟠 **Proxied (оранжевое облачко)** — трафик идёт через CF-серверы. CF терминирует TLS у себя, к твоему серверу приходит уже расшифрованный трафик или пере-шифрованный CF-сертификатом. Реальный IP твоего сервера скрыт.
- ⚪ **DNS only (серое облачко)** — CF просто отвечает на DNS-запросы, но не проксирует трафик. Клиент напрямую идёт к твоему серверу.

**Для этого юнита:**
- **Для HTTP-01 challenge** → **обязательно DNS only** (серое облачко). Иначе LE будет разговаривать с CF, а не с твоим сервером, и валидация не пройдёт (либо CF отдаст свой автосгенерированный сертификат, что тоже некорректно).
- **Для DNS-01 challenge** — можно и proxied, и DNS only. LE проверяет только DNS, не HTTP.
- **В юните 11** мы вернёмся к proxied режиму и настроим Origin Certificate от CF.

### Теория (2ч) 📺

- [ ] Reverse proxy: 3+ причины использовать (см. эксперимент 1)
- [ ] TLS handshake — концептуально (не крипту, а последовательность: ClientHello → ServerHello → Certificate → Key Exchange → Finished)
- [ ] Разница `location /api` vs `location /api/` vs `location ~ ^/api` (три типа матчинга: prefix, exact, regex)
- [ ] Что такое SNI (Server Name Indication) — как один IP отдаёт разные сертификаты для разных доменов

**Рекомендую:**
- Официальная документация nginx: `nginx.org/en/docs/beginners_guide.html`
- Видео "TLS Handshake Explained" — любой канал, 15 минут
- `man certbot` — попытайся читать на английском (см. словарь ниже)

---

### Эксперимент 1 🧠 — Установка nginx + первый статический сайт

**Что делаем:** поднимаем nginx с нуля, отдаём простую HTML-страничку.

**Команды:**
```bash
# Установка
sudo apt update && sudo apt install nginx -y

# Проверка что установился и запустился
systemctl status nginx
ss -tlnp | grep nginx

# Посмотреть дефолтный конфиг
cat /etc/nginx/nginx.conf
ls /etc/nginx/sites-enabled/
ls /etc/nginx/sites-available/

# Открой в браузере http://IP-твоего-сервера — увидишь "Welcome to nginx!"
curl -v http://localhost/
```

**Что сделать:**
1. Создай директорию `/var/www/eakramar.ru/` и положи туда `index.html` с любым содержимым (твоя визитка / "Hello from eakramar")
2. Создай **свой server block** в `/etc/nginx/sites-available/eakramar.ru.conf`:
   ```nginx
   server {
       listen 80;
       server_name eakramar.ru www.eakramar.ru;
       root /var/www/eakramar.ru;
       index index.html;
       
       access_log /var/log/nginx/eakramar.access.log;
       error_log /var/log/nginx/eakramar.error.log;
   }
   ```
3. Активируй: `sudo ln -s /etc/nginx/sites-available/eakramar.ru.conf /etc/nginx/sites-enabled/`
4. Отключи дефолтный: `sudo rm /etc/nginx/sites-enabled/default`
5. Проверь синтаксис (**обязательно перед reload!**): `sudo nginx -t`
6. Перечитай конфиг: `sudo systemctl reload nginx`
7. Проверь: `curl -v -H "Host: eakramar.ru" http://IP-твоего-сервера/`

**Что записать в `notes/unit-04-nginx-https.md`:**
- Зачем в nginx разделили `sites-available/` и `sites-enabled/`? (Это конвенция Debian/Ubuntu, не самого nginx)
- Что делает `nginx -t` и почему **всегда** запускать перед reload?
- В чём разница `systemctl reload nginx` vs `systemctl restart nginx`? (Reload — плавно перечитывает конфиг, соединения не рвутся. Restart — рвёт все.)
- В логах `/var/log/nginx/access.log` найди свой curl-запрос. Какой формат по умолчанию? (combined log format)

**Вопросы для самопроверки:**
- Что произойдёт если два server block'а слушают один порт с одинаковым server_name?
- Что произойдёт если нет ни одного server block с `server_name eakramar.ru`, но запрос пришёл с этим Host заголовком? (Ответ: сработает default_server, обычно первый по порядку)

---

### Эксперимент 2 🧠 — HTTPS через Let's Encrypt (HTTP-01 challenge)

**Что делаем:** получаем валидный TLS-сертификат от Let's Encrypt.

**Подготовка:**
1. Убедись что в Cloudflare A-запись eakramar.ru → IP твоего сервера, режим **DNS only** (серое облачко) — критично для HTTP-01
2. Проверь: `dig eakramar.ru +short` — должен вернуть IP твоего сервера, не IP Cloudflare (104.x.x.x / 172.x.x.x — это CF)
3. Открой 80 и 443 порты в firewall провайдера / ufw если он у тебя уже включён

**Команды:**
```bash
# Устанавливаем certbot
sudo apt install certbot python3-certbot-nginx -y

# Получаем сертификат с автоматической правкой nginx-конфига
sudo certbot --nginx -d eakramar.ru -d www.eakramar.ru

# Certbot задаст вопросы: email (для уведомлений об expiration), согласие с ToS, 
# хочешь ли редирект HTTP → HTTPS (говори YES)

# Смотрим что сделал certbot с твоим конфигом
cat /etc/nginx/sites-available/eakramar.ru.conf

# Проверяем сертификаты
sudo certbot certificates

# Тест автообновления (dry-run — не портит реальный сертификат)
sudo certbot renew --dry-run
```

**Что сделать вручную:**
- Открой https://eakramar.ru в браузере → должен быть зелёный замок
- Проверь оценку на https://www.ssllabs.com/ssltest/ → должно быть A или A+

**Что записать:**
- Куда certbot положил сертификаты? (`/etc/letsencrypt/live/eakramar.ru/`)
- Какие файлы там лежат? Что каждый значит?
  - `fullchain.pem` — твой сертификат + intermediate certs
  - `privkey.pem` — приватный ключ (**никому не показывать, никогда не коммитить**)
  - `cert.pem` — только твой сертификат (обычно не используется)
  - `chain.pem` — только intermediate (без твоего)
- Как certbot настроил автообновление? Проверь: `systemctl list-timers | grep certbot`
- Что делает `--deploy-hook` в конфиге renewal? Найди файл `/etc/letsencrypt/renewal/eakramar.ru.conf`

**Вопросы:**
- Сертификат LE действителен 90 дней. Когда именно certbot будет пытаться обновить? (за 30 дней до expiration, попытки через systemd timer)
- Что произойдёт если сертификат **уже истёк** и certbot renew падает? (Браузеры покажут ошибку. Восстановление: устрани причину падения renew, потом `certbot renew --force-renewal`)

**⚠️ Если HTTP-01 не сработал:**
Переходи на DNS-01 через Cloudflare API (эксперимент 2b).

---

### Эксперимент 2b 🧠 — [опционально] DNS-01 challenge через Cloudflare API

**Когда делать:** если HTTP-01 из эксперимента 2 упал с ошибкой валидации, или хочешь научиться DNS-01 в принципе.

**Что делаем:** получаем сертификат без открытого 80 порта, через DNS-запись.

**Подготовка:**
1. Создай Cloudflare API токен: **My Profile → API Tokens → Create Token → Edit zone DNS template**. Ограничь скоуп только зоной eakramar.ru
2. Сохрани токен в файл на сервере с правами 600:
   ```bash
   sudo mkdir -p /etc/letsencrypt/secrets
   sudo tee /etc/letsencrypt/secrets/cloudflare.ini <<EOF
   dns_cloudflare_api_token = ТВОЙ_ТОКЕН_СЮДА
   EOF
   sudo chmod 600 /etc/letsencrypt/secrets/cloudflare.ini
   ```

**Команды:**
```bash
sudo apt install python3-certbot-dns-cloudflare -y

# Получаем сертификат через DNS-01
sudo certbot certonly \
  --dns-cloudflare \
  --dns-cloudflare-credentials /etc/letsencrypt/secrets/cloudflare.ini \
  -d eakramar.ru -d '*.eakramar.ru'

# Обрати внимание: с DNS-01 можешь получить wildcard-сертификат (*.eakramar.ru)!
# С HTTP-01 wildcard нельзя.
```

Потом руками правишь конфиг nginx, добавляя пути к сертификату.

**Что записать:**
- Почему DNS-01 позволяет wildcard, а HTTP-01 — нет? (HTTP-01 доказывает контроль над **конкретным именем**, DNS-01 — контроль над **всей зоной**)
- Какие ещё DNS-провайдеры поддерживает certbot? (`certbot-dns-*` — есть под большинство: Route53, GoDaddy, DigitalOcean, Hetzner и т.д.)

---

### Эксперимент 3 🧠 — Reverse proxy: nginx → приложение

**Что делаем:** поднимаем простое приложение на localhost:3000 и делаем nginx его reverse proxy.

**Простое приложение** (Python встроенный HTTP-сервер, ничего ставить не надо):
```bash
# В одном терминале — сервер
cd /tmp && python3 -m http.server 3000

# Работает? Проверь: curl http://localhost:3000
```

**Nginx-конфиг для reverse proxy** — добавь в eakramar.ru.conf в HTTPS server-блок:
```nginx
location /app/ {
    proxy_pass http://localhost:3000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

Перечитай: `sudo nginx -t && sudo systemctl reload nginx`.
Проверь: `curl https://eakramar.ru/app/` — должен вернуть содержимое `/tmp` (то что отдаёт Python-сервер).

**Ключевой эксперимент — слеш в proxy_pass:**

Сделай **два разных теста**:

**Тест А:** `proxy_pass http://localhost:3000;` (**без** слеша в конце)
```bash
curl -v https://eakramar.ru/app/
# Смотри access.log Python-сервера — какой path запрашивался?
```

**Тест Б:** `proxy_pass http://localhost:3000/;` (**со** слешом в конце)
```bash
curl -v https://eakramar.ru/app/
# Опять смотри access.log Python-сервера
```

**Что должно произойти:**
- Тест А (без слеша): Python-сервер получит `GET /app/`
- Тест Б (со слешом): Python-сервер получит `GET /` — префикс `/app/` **обрезан** nginx-ом

Это **классический источник багов** в nginx. Запомни правило: слеш в конце `proxy_pass` = обрезать location prefix.

**Что ещё записать:**
- Что делают заголовки `X-Real-IP`, `X-Forwarded-For`, `X-Forwarded-Proto` и зачем их передавать? (Без них backend-приложение будет думать что все запросы идут с 127.0.0.1)
- Что такое `$host` vs `$server_name` в nginx? (host — то что клиент прислал в Host заголовке; server_name — то что в конфиге)

**Вопросы:**
- Если приложение на localhost:3000 упало — что покажет nginx клиенту? (502 Bad Gateway)
- Что покажет если приложение долго не отвечает? (504 Gateway Timeout, по умолчанию 60 секунд)

---

### Эксперимент 4 📺 — gzip, security headers, оценка на SSL Labs

**Что делаем:** доводим сайт до grade A+ на SSL Labs и securityheaders.com.

**Добавь в HTTPS server-блок:**
```nginx
# Gzip compression
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
gzip_min_length 1000;

# Security headers
add_header Strict-Transport-Security "max-age=300" always;
add_header X-Frame-Options "DENY" always;
add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Content-Security-Policy "default-src 'self'" always;

# TLS настройки (если certbot не поставил — добавь)
ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;
```

**Внимание про HSTS:** я специально ставлю `max-age=300` (5 минут) для эксперимента. Это правильная стратегия при первом включении. **Не ставь max-age=31536000 (год) сразу** — если что-то пойдёт не так с сертификатом, все посетители, которые уже "получили" HSTS, не смогут зайти на HTTP-версию, пока не истечёт срок.

**Проверка:**
```bash
# gzip работает?
curl -H "Accept-Encoding: gzip" -I https://eakramar.ru/

# Security headers присутствуют?
curl -I https://eakramar.ru/
```

**Онлайн-проверки:**
- https://www.ssllabs.com/ssltest/ — должно быть A+
- https://securityheaders.com/ — должно быть минимум B, лучше A

**Что записать:**
- Какие security headers ты добавил и что каждый делает?
- Почему `Content-Security-Policy` для реального сайта — сложная штука? (Он режет inline JS, внешние ресурсы. Нужно точно знать что использует твой фронт)
- Что покажет SSL Labs если у тебя всё хорошо? (Grade A+, оценки Certificate/Protocol/Key Exchange/Cipher Strength)

**Вопросы:**
- Что такое **HSTS preload**? Стоит ли включать? (Это когда браузер знает что твой домен всегда HTTPS **до первого визита**. Опасно: если однажды захочешь HTTP — забудь. Только когда точно уверен)
- Почему `always` в `add_header`? (Без always заголовок отдаётся только на 200-ответы. С always — на все, включая 4xx/5xx. Обычно надо always.)

---

### Эксперимент 5 🧠 — Диагностика: сайт не работает, что делать?

**Что делаем:** сознательно ломаем сайт разными способами и учимся диагностировать. Это тренировка "на дежурстве".

**Сценарий А — синтаксическая ошибка в конфиге:**
```bash
# Внеси очевидную ошибку — сотри точку с запятой в конце одной строки
sudo nano /etc/nginx/sites-available/eakramar.ru.conf
sudo nginx -t
# Что показывает? Точно указывает файл и строку?
# Верни точку с запятой на место
```

**Сценарий Б — порт занят:**
```bash
# Что если 80 порт занял кто-то другой? Симулируем:
sudo systemctl stop nginx
sudo python3 -m http.server 80 &  # (в фоне, будет держать порт)
sudo systemctl start nginx  # что произойдёт?
sudo journalctl -u nginx -n 20
# Убей python
sudo kill %1
sudo systemctl start nginx  # теперь нормально
```

**Сценарий В — сертификат "истёк":**
Не будем реально истекать сертификат, но научимся смотреть:
```bash
# Когда истекает?
sudo openssl x509 -in /etc/letsencrypt/live/eakramar.ru/fullchain.pem -noout -dates

# Все детали сертификата
sudo openssl x509 -in /etc/letsencrypt/live/eakramar.ru/fullchain.pem -noout -text | head -30

# Проверить сертификат снаружи (как видит клиент)
echo | openssl s_client -connect eakramar.ru:443 -servername eakramar.ru 2>/dev/null | openssl x509 -noout -dates
```

**Сценарий Г — nginx не отвечает клиенту:**
Пройди чеклист:
```bash
# 1. Слушает ли nginx нужный порт?
sudo ss -tlnp | grep -E ':(80|443)'

# 2. Синтаксис конфига?
sudo nginx -t

# 3. Логи nginx?
sudo tail -20 /var/log/nginx/error.log
sudo journalctl -u nginx -n 20

# 4. Firewall пропускает?
sudo ufw status  # если ufw используется

# 5. С другой машины — доходит ли трафик?
# С локальной машины:
curl -v https://eakramar.ru/
telnet eakramar.ru 443  # или ncat -v eakramar.ru 443

# 6. DNS резолвится куда надо?
dig eakramar.ru +short  # должен быть IP твоего сервера, не CF
```

**Что записать в раздел "Ключевые выводы для дежурства":**
- **Не переопределения**, а **действия** в стиле "если Х — сделай Y":
  - "Сайт не открывается → в первую очередь `nginx -t` + `journalctl -u nginx -n 50`"
  - "502 Bad Gateway → backend упал, проверь `ss -tlnp | grep :PORT` — слушает ли твой upstream"
  - "504 Gateway Timeout → backend жив но тормозит, крути `proxy_read_timeout`"
  - "Сертификат — проверь дату: `openssl x509 -in /path -noout -dates`"
  - "После правки конфига **всегда** `nginx -t` перед `reload`"

---

### Артефакты для Git

```
artifacts/unit-04-nginx/
├── eakramar.ru.conf          # финальный nginx-конфиг для домена (без секретов)
└── (при желании: демо-приложение)

notes/unit-04-nginx-https.md   # все эксперименты + ключевые выводы для дежурства
```

**Важно про безопасность:**
- **НЕ коммить** содержимое `/etc/letsencrypt/live/eakramar.ru/privkey.pem`
- **НЕ коммить** `cloudflare.ini` с API-токеном
- В конфиге nginx можно оставить пути к сертификатам — сами файлы там не лежат

### Тест юнита 4

1. 4 причины использовать reverse proxy.
2. Что делает HSTS? Риск большого max-age?
3. `proxy_pass http://localhost:3000` vs `proxy_pass http://localhost:3000/` (со слешом).
4. Как настроить автообновление LE-сертификата, чтобы не забылось?
5. Сайт работает по HTTP, на HTTPS — connection refused. Что проверять?
6. **Новый:** отличие HTTP-01 challenge от DNS-01. Когда какой использовать?

<details><summary>Эталонные ответы</summary>

1. (а) TLS termination (одно место, где живут сертификаты); (б) load balancing (nginx умеет upstream с несколькими backend'ами); (в) кэширование ответов и статики; (г) rate limiting и защита от DDoS/bruteforce; (д) compression (gzip/brotli); (е) единая точка логов и метрик; (ж) host-based / path-based routing на разные приложения; (з) TLS-параметры (протоколы, cipher-suites) — backend не касается.

2. HSTS (Strict-Transport-Security) говорит браузеру ходить только по HTTPS на `max-age` секунд. Записывается в браузер, действует независимо от твоего сервера. **Риск большого max-age:** если сертификат сломается или ты специально захочешь откатиться на HTTP — пользователи не смогут зайти до истечения срока. Стратегия выкатки: max-age=300 → проверка недели → max-age=86400 → месяц → max-age=31536000 (год).

3. **Без слеша:** `proxy_pass http://localhost:3000;` — nginx передаёт URI как есть. Запрос `/app/users` уйдёт как `/app/users`. **Со слешом:** `proxy_pass http://localhost:3000/;` — nginx **обрезает** location prefix. При `location /app/`, запрос `/app/users` уйдёт как `/users`. Классический источник багов.

4. Certbot автоматически ставит systemd timer при установке. Проверить: `systemctl list-timers | grep certbot`. Обязательно тестируй: `certbot renew --dry-run` — должен пройти без ошибок. Для nginx-плагина reload происходит автоматически; для manual — настрой `--deploy-hook 'systemctl reload nginx'`. Renewal-конфиг в `/etc/letsencrypt/renewal/DOMAIN.conf`.

5. По порядку: (а) `ss -tlnp | grep :443` — слушает ли nginx 443; (б) `nginx -t` — синтаксис конфига OK?; (в) `journalctl -u nginx -n 50` — есть ли ошибки в логах systemd; (г) `tail /var/log/nginx/error.log` — nginx-specific ошибки; (д) `ufw status` / firewall провайдера — пропускает 443?; (е) в конфиге есть `listen 443 ssl;`?; (ж) пути к `ssl_certificate` и `ssl_certificate_key` существуют и читаемы nginx-процессом?; (з) с другой машины `curl -v https://...` — доходит ли трафик до порта.

6. **HTTP-01:** LE делает HTTP-запрос к `http://твой-домен/.well-known/acme-challenge/TOKEN`, сервер отдаёт ожидаемый контент. Требования: 80 порт открыт извне, LE-серверы могут достучаться. Плюсы: просто. Минусы: не даёт wildcard-сертификаты; не работает если сервер не доступен извне (private инфра, российский VPS с блокировкой). **DNS-01:** ты (через плагин) создаёшь TXT-запись `_acme-challenge.твой-домен`, LE проверяет DNS. Требования: доступ к DNS через API. Плюсы: работает без открытого сервера, даёт wildcard. Минусы: сложнее настроить (API-токен, credentials-файл). Когда HTTP-01: простой сайт, публичный сервер, не нужен wildcard. Когда DNS-01: private инфра, wildcard-сертификаты, недоступность LE к серверу.

</details>

---

### 📖 Мини-словарь технического английского для юнита 4

Формат тот же что в юните 3: делай Anki-карточки по 5-7 в день из тех слов, которые реально встретишь в man/документации.

#### 🔐 TLS / Сертификаты — специфика

| Английский | Русский | Где встречается |
|------------|---------|-----------------|
| **certificate** | сертификат | certbot output |
| **certificate authority (CA)** | центр сертификации | Let's Encrypt = CA |
| **issue a certificate** | выдать сертификат | "issuing certificate for..." |
| **revoke** | отозвать | revoked certificate |
| **expire / expiration** | истечь / истечение | "certificate will expire" |
| **renew** | продлить | `certbot renew` |
| **wildcard certificate** | wildcard-сертификат (`*.domain`) | покрывает все поддомены |
| **subject** | субъект (кому выдан) | CN=eakramar.ru |
| **issuer** | издатель (кто выдал) | Let's Encrypt R3 |
| **chain / chain of trust** | цепочка (доверия) | root → intermediate → leaf |
| **root certificate** | корневой сертификат | предустановлен в ОС/браузер |
| **intermediate** | промежуточный | между root и твоим |
| **leaf certificate** | конечный (твой) сертификат | в конце цепочки |
| **fingerprint** | отпечаток (хеш серта) | для сверки подлинности |
| **key exchange** | обмен ключами | часть TLS handshake |
| **cipher suite** | набор шифров | согласуется в handshake |
| **forward secrecy** | прямая секретность | свойство современных cipher suites |
| **SNI (Server Name Indication)** | указание имени сервера в TLS | как один IP держит много доменов |

#### 🔧 Nginx-специфика

| Английский | Русский | Контекст |
|------------|---------|----------|
| **server block** | server-блок | секция `server {}` в конфиге |
| **location block** | location-блок | `location /path/ {}` |
| **upstream** | апстрим (backend-сервер) | `upstream backend {}` |
| **reload** | перечитать конфиг | `nginx -s reload` |
| **restart** | перезапустить (соединения рвутся) | `systemctl restart nginx` |
| **backend** | бэкенд (твоё приложение) | что стоит за reverse proxy |
| **origin server** | origin-сервер (реальный, не кэш/CDN) | Cloudflare терминология |
| **worker process** | рабочий процесс nginx | обычно = кол-во CPU |
| **directive** | директива (строчка конфига) | `listen`, `server_name` |
| **prefix match** | совпадение по префиксу | `location /api` |
| **regex match** | совпадение по регулярке | `location ~ ^/api` |
| **default_server** | сервер по умолчанию | если нет матча по Host |

#### 🌐 HTTP / веб — общие термины

| Английский | Русский | Где |
|------------|---------|-----|
| **request** | запрос | HTTP request |
| **response** | ответ | HTTP response |
| **status code** | код статуса | 200, 404, 502 |
| **redirect** | редирект (перенаправление) | 301, 302 |
| **header** | заголовок | HTTP header |
| **body / payload** | тело ответа | HTML/JSON |
| **query string** | строка запроса | `?key=value&...` |
| **user agent** | клиент (браузер) | User-Agent header |
| **origin** | источник | Origin/Referer headers |
| **cross-origin** | кросс-доменный | CORS |
| **preflight** | предзапрос (OPTIONS в CORS) | CORS preflight |
| **cache-control** | управление кэшем | Cache-Control header |
| **etag** | тег версии контента | If-None-Match |
| **compression** | сжатие | gzip, brotli |
| **rate limiting** | ограничение скорости запросов | защита от DDoS |
| **throttling** | замедление (rate limiting в мягкой форме) | вместо блокировки — задержка |

#### 🛡️ Безопасность — заголовки

| Английский | Русский | |
|------------|---------|--|
| **HSTS (Strict-Transport-Security)** | принудительный HTTPS | max-age в секундах |
| **CSP (Content Security Policy)** | политика источников контента | режет XSS |
| **CORS (Cross-Origin Resource Sharing)** | доступ с других доменов | Access-Control-* headers |
| **X-Frame-Options** | защита от clickjacking | DENY / SAMEORIGIN |
| **X-Content-Type-Options** | запрет угадывания MIME | nosniff |
| **Referrer-Policy** | политика передачи Referer | strict-origin-when-cross-origin |
| **XSS (Cross-Site Scripting)** | межсайтовый скриптинг | атака |
| **CSRF (Cross-Site Request Forgery)** | подделка межсайтовых запросов | атака |
| **MITM (Man-In-The-Middle)** | атака "человек посередине" | TLS защищает |

#### Готовые 10 Anki-карточек на старт

1. `certificate expires in 90 days` → сертификат истекает через 90 дней (Let's Encrypt default)
2. `certificate chain of trust` → цепочка доверия (root → intermediate → leaf)
3. `reload nginx` → перечитать конфиг без разрыва соединений
4. `502 Bad Gateway` → upstream/backend не отвечает или упал
5. `504 Gateway Timeout` → backend жив, но не отвечает за timeout
6. `SNI (Server Name Indication)` → как один IP отдаёт разные сертификаты
7. `wildcard certificate` → сертификат для `*.domain` (только через DNS-01)
8. `HSTS max-age` → сколько секунд браузер помнит "только HTTPS"
9. `to issue a certificate` → выдать сертификат
10. `to renew a certificate` → продлить сертификат


---

## Юнит 5 📺 — Firewall, hardening

**Тип нагрузки:** относительно лёгкий, можно вечером

**Теория (3ч):** 📺
- [ ] iptables vs nftables vs ufw
- [ ] Базовый hardening Ubuntu

**Практика (4ч):**
- [ ] ufw: только SSH-порт, 80, 443
- [ ] fail2ban для SSH
- [ ] unattended-upgrades
- [ ] Чек-лист "Initial server setup" в Git

**Артефакт:** чек-лист + конфиги fail2ban

### Тест юнита 5

1. Команда: какие IP сейчас забанены fail2ban?
2. ufw vs iptables vs nftables — что под капотом, что выбрать в 2026?
3. Что произойдёт, если включить ufw без `ufw allow 22`?
4. SSH bastion/jump host — зачем, схема.
5. 5 правил hardening Ubuntu по умолчанию.

<details><summary>Эталонные ответы</summary>

1. `fail2ban-client status sshd`. Или `iptables -L f2b-sshd -n`.
2. iptables — старый user-space к netfilter. nftables — новый, заменяет iptables. ufw — обёртка. 2026: ufw для простых случаев, nftables напрямую для сложных.
3. **Потеряешь SSH-доступ.** Порядок: `ufw allow 22` → `ufw enable`. Имей под рукой web-консоль.
4. Bastion — единственный сервер, доступный из интернета по SSH. Остальные — только из private. `ssh -J bastion target`.
5. (1) SSH key only; (2) non-root user with sudo; (3) ufw минимум портов; (4) fail2ban; (5) unattended-upgrades; (6) поменян SSH-порт; (7) отключены неиспользуемые сервисы.

</details>

---

## Юнит 6 🧠 — Bash скриптинг

**Тип нагрузки:** свежая голова — пишем серьёзный скрипт

**Теория (2ч):** 📺
- [ ] "Bash Guide for Beginners" (TLDP)
- [ ] ShellCheck

**Практика (5ч):** 🧠
- [ ] `server-init.sh`, автоматизирующий юниты 1-5
- [ ] Аргументы, `set -euo pipefail`, логирование, **идемпотентность**
- [ ] ShellCheck — 0 warnings

**Артефакт:** `server-init.sh` в Git, работает на свежем VPS

### Тест юнита 6

1. Что делает `set -euo pipefail`? Зачем как первая строка?
2. Что такое идемпотентность? Пример НЕ-идемпотентного и фикс.
3. `$@` vs `$*` (с кавычками и без).
4. Как обработать ошибку и сделать cleanup (удалить tmp-файлы)?
5. Однострочник: пинговать IP из файла, выводить только недоступные.

<details><summary>Эталонные ответы</summary>

1. `-e` — exit при ошибке; `-u` — ошибка при использовании неинициализированной переменной; `-o pipefail` — exit code пайплайна = последней упавшей команды.
2. Идемпотентность = можно запускать многократно с тем же результатом. **НЕ:** `useradd deploy`. **Да:** `id deploy &>/dev/null || useradd deploy`.
3. `"$@"` — каждый аргумент отдельным словом. `"$*"` — склеены в одну строку. **Используй всегда `"$@"`**.
4. `TMPDIR=$(mktemp -d); trap 'rm -rf "$TMPDIR"' EXIT`
5. `while read -r ip; do ping -c1 -W1 "$ip" &>/dev/null || echo "$ip"; done < ips.txt`

</details>

---

## Юнит 7 📺 — Git продвинутый

**Тип нагрузки:** средний, можно вечером

**Теория (3ч):** 📺
- [ ] "Pro Git" — главы 2, 3, 7
- [ ] Видео "Git Internals"

**Практика (4ч):**
- [ ] Репо A-way-to-DevOps, всё туда
- [ ] `rebase -i`, `cherry-pick`, `reflog`, `bisect`
- [ ] Pre-commit hook со ShellCheck
- [ ] Git flow vs trunk-based

**Артефакт:** репо с осмысленной историей, pre-commit hook

### Тест юнита 7

1. `git merge` vs `git rebase`? Когда что?
2. Случайный `git reset --hard`. Как восстановить?
3. Detached HEAD — что, чем опасно, как выйти?
4. 10 коммитов в feature, многие — фиксы опечаток. Как причесать перед merge?
5. `git pull` vs `git pull --rebase`. Что лучше в команде?

<details><summary>Эталонные ответы</summary>

1. Merge — merge-коммит, нелинейная история. Rebase — переписывает поверх, линейная. **Никогда не rebase ветки, на которые работают другие.**
2. `git reflog` → находишь SHA → `git reset --hard <SHA>`.
3. HEAD указывает на коммит, не на ветку. Опасно: коммиты не принадлежат ветке. Выход: `git switch -c new-branch`.
4. `git rebase -i HEAD~10` → `pick`/`squash`/`fixup`/`reword`/`drop`.
5. `pull --rebase` лучше в команде. В команде: `pull.rebase = true` глобально.

</details>

---

## Юнит 8 🔁 — Резерв / закрепление / Anki

**Тип нагрузки:** идеально для вахты или конца межвахты, когда устал

Используй для:
- [ ] Догнать пропущенное
- [ ] Углубиться в интересную тему
- [ ] Создать первые 50-70 Anki-карточек по пройденному
- [ ] Отдохнуть

Если успел и не устал: tmux, dotfiles-репо.

### Тест юнита 8 — повторение

5 случайных вопросов из юнитов 1-7 (выбери сам или попроси меня).

---

## Юнит 9 🧠 — Проект 1: статический сайт с CI/CD

**Тип нагрузки:** свежая голова — собираем всё пройденное в работающий проект

- [ ] Репо `personal-site`
- [ ] Простой статический сайт (HTML/CSS или Hugo/Jekyll)
- [ ] GitHub Actions: push в main → деплой на VPS через SSH
- [ ] README с архитектурой и инструкциями
- [ ] Сайт по HTTPS на твоём домене

### Тест юнита 9

1. Где хранить SSH-ключ для деплоя из GitHub Actions?
2. GitHub Actions secrets — что это, кто может прочитать?
3. Как запускать workflow только при push в main, не при PR?
4. После деплоя сайт показывает старое. 3 причины.
5. `rsync -av` vs `rsync -avz --delete`. Риски `--delete`?

<details><summary>Эталонные ответы</summary>

1. **GitHub Secrets**. В workflow подгружается в `~/.ssh/id_ed25519`. **Никогда не коммить приватный ключ.**
2. Зашифрованные переменные, `${{ secrets.NAME }}`. Не видны после сохранения, маскируются в логах. PR из форков не имеют доступа.
3. `on: push: branches: [main]`
4. (а) Браузер кэш; (б) CDN-кэш; (в) nginx кэш — `nginx -s reload`; (г) symlink не переключился; (д) sticky session.
5. `--delete` удаляет на target отсутствующие в source. Риск: ошибка в путях = удалишь продакшен. Перед `--delete` всегда `--dry-run`.

</details>

---

## Юнит 10 🧠 — PostgreSQL основы

**Тип нагрузки:** свежая голова, БД — серьёзная тема

**Теория (3ч):** 📺
- [ ] PostgreSQL, базовые psql команды
- [ ] `pg_dump`, `pg_restore`, концепция PITR

**Практика (4ч):** 🧠
- [ ] Установка PostgreSQL
- [ ] БД, пользователь, таблица, данные
- [ ] Скрипт ежедневного бэкапа + ротация (7 дней)
- [ ] **Восстанови из бэкапа на чистом сервере** — критично

**Артефакт:** скрипты бэкапа и восстановления в Git

### Тест юнита 10

1. `pg_dump -Fc` vs обычный. Что предпочесть и почему?
2. WAL — что это, зачем для бэкапов?
3. Создай read-only пользователя в PostgreSQL.
4. Бэкапы 50GB ежедневно, диск полный через месяц. 3 стратегии.
5. Команда: какие запросы сейчас выполняются?

<details><summary>Эталонные ответы</summary>

1. `-Fc` — custom format (бинарный, сжатый). Можно восстанавливать выборочно, параллельно. **В продакшене всегда `-Fc`** или `-Fd`.
2. Write-Ahead Log — журнал транзакций до применения. Crash recovery, репликация, **PITR**.
3. ```sql
CREATE USER readonly WITH PASSWORD '...';
GRANT CONNECT ON DATABASE mydb TO readonly;
\c mydb
GRANT USAGE ON SCHEMA public TO readonly;
GRANT SELECT ON ALL TABLES IN SCHEMA public TO readonly;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO readonly;
```
4. (а) Ротация (`find /backups -mtime +7 -delete`); (б) инкрементальные (pgbackrest, barman); (в) S3 с lifecycle; (г) zstd-сжатие.
5. ```sql
SELECT pid, usename, state, query_start, query 
FROM pg_stat_activity WHERE state != 'idle' ORDER BY query_start;
```

</details>

---

## Юнит 11 📺 — DNS глубже, CDN, email

**Тип нагрузки:** средний

**Теория (3ч):** 📺
- [ ] SPF, DKIM, DMARC
- [ ] CDN (Cloudflare)

**Практика (4ч):**
- [ ] Cloudflare на домен (уже подключён)
- [ ] **Здесь переключаемся на proxied** (оранжевое облачко)
- [ ] Кэширование, page rules
- [ ] (Опц.) Postfix + SendGrid/Mailgun free tier

### Тест юнита 11

1. Что делает SPF, как проверяется получателем?
2. DKIM vs SPF — принципиальная разница, что вместе закрывают?
3. Cloudflare proxied — плюсы и минусы.
4. A-запись 1.2.3.4, в CF proxied. Что покажет `dig` и почему?
5. DMARC `p=quarantine` vs `p=reject` — когда что?

<details><summary>Эталонные ответы</summary>

1. SPF — TXT-запись с разрешёнными отправителями. Получатель смотрит Return-Path → SPF домена → проверяет IP в списке.
2. SPF — **откуда** (IP). DKIM — **что контент не изменён**, подписан приватным ключом домена. Вместе: имеет право отправлять + контент не подделан. DMARC объединяет.
3. Плюсы: DDoS, кэш, WAF, скрытие реального IP. Минусы: CF видит весь HTTPS-трафик; не-HTTP порты ломаются.
4. Покажет **IP Cloudflare**. В proxied A-запись подменяется anycast-IP CF.
5. `quarantine` — в спам. `reject` — отклонить. Стратегия: `p=none` → `p=quarantine pct=10` → `p=reject`.

</details>

---

## Юнит 12 🧠 — Аттестация Этапа 1

**Тип нагрузки:** марафон на свежую голову — выделить полный день

- [ ] **Снеси VPS/виртуалку.** Подними новый. Через `server-init.sh` доведи до production-ready за один прогон.
- [ ] Разверни `personal-site` через CI/CD, БД с бэкапами
- [ ] Пост в README репо A-way-to-DevOps — что освоил
- [ ] Пришли мне план, лог, ссылку на GitHub
- [ ] **Этапный экзамен**: 15 вопросов + практика (2 часа)

Сдан → Этап 2. Пробелы → 1-2 недели доработки.

---

# ЭТАПЫ 2-6 (обзор)

Детальный план каждого этапа выдам после завершения предыдущего.

| Этап | Юниты | Ключевое |
|------|-------|----------|
| 2. Контейнеры | 13-28 | Docker глубоко, Kubernetes, CKA |
| 3. CI/CD + IaC | 29-44 | GitLab CI, Terraform, Ansible |
| 4. Облака + Observability | 45-64 | AWS SAA, Prometheus, Grafana |
| 5. Поиск работы | парал. с 4 | Резюме, портфолио, интервью |
| 6. Senior на работе | — | Service mesh, GitOps, platform |

---

## Контрольные точки — без жёстких сроков

Раньше я ставил даты по месяцам. С твоим графиком 30/30 это не имеет смысла. Считаем **по событиям, не по календарю**:

| Веха | Что готово |
|------|------------|
| Этап 1 закрыт | Linux, сети, nginx, bash, git, БД, проект 1 |
| Этап 2 закрыт | Docker + Kubernetes (CKA) + проект 2-3 |
| Этап 3 закрыт | Terraform + Ansible + GitLab CI + проект 4 |
| Готов к откликам | После этапа 3 + AWS SAA параллельно |
| Junior offer | После этапа 4 + активный поиск |
| Middle | 1.5-2 года реальной работы |
| Senior | 3-4 года реальной работы |

Когда увидим реальный темп после 2-3 циклов — пересчитаем в календарных датах.

---

## Принципы

1. **Учусь когда есть силы.** Не "по графику любой ценой".
2. **5 инструментов глубоко > 20 поверхностно.**
3. **Каждая тема → артефакт в Git.**
4. **Документация > туториалы > видео.**
5. **Английский по умолчанию.**
6. **Проблемы — материал для обучения.**
7. **Не обманывай себя на тестах.** Гугл/AI на тестах = выстрел в ногу.
8. **Самооценку ставь после теста**, не до.
9. **Не делай больше плана.** Расфокус удлиняет каждый юнит.
10. **На вахте — только Anki.** Никакого "штудирования вечером".
11. **Усталость > план.** Если сегодня нет сил — закрываешь ноутбук без вины.
